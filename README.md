# signature-assets

Permanent image hosting for Email Signature Studio (GitHub Pages).

**Never delete or rename files here** — every file is referenced by live email signatures. Tweaks overwrite the same path; redesigns get a new filename.
